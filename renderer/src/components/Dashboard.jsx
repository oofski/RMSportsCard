// STUB — replaced by the implementation agent. Keeps the build green meanwhile.
import React from 'react'
export default function Dashboard(props) {
  return <div className="card">Dashboard (pending implementation)</div>
}
